export const letters=(n)=>{let s='';n++;while(n>0){const m=(n-1)%26;s=String.fromCharCode(65+m)+s;n=Math.floor((n-1)/26);}return s;};export const clamp=(v,lo,hi)=>Math.max(lo,Math.min(hi,v));export const copyToClipboard=async(text)=>{try{await navigator.clipboard.writeText(text);}catch(e){console.warn('Clipboard write failed',e);}};export const readFromClipboard=async()=>{try{return await navigator.clipboard.readText();}catch(e){console.warn('Clipboard read failed',e);return ''}};export const parseTSV=(tsv)=>tsv.replace(/\r/g,'').split('\n').map(r=>r.split('\t'));export const toTSV=(rows)=>rows.map(r=>r.map(c=>c??'').join('\t')).join('\n');